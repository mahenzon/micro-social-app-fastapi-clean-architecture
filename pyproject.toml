[project]
name = "micro-social-app"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "social-backend",
]

[tool.uv.workspace]
members = [
    "social-backend",
]

[tool.uv.sources]
social-backend = { workspace = true }
